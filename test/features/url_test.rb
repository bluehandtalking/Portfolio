require 'test_helper'

feature  "As a parent, I want to see the good work my kid does so I can show it off to my friend" do
  describe "When I go to the root url, my browser will navigate to the root URI" do
    # uri = URI.parse(current_url) 
    # current_url.should == root_url
    # "#{uri.path}?#{uri.query}".should == people_path(:search => 'name') 
  end
end

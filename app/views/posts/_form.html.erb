<%= form_for(@post) do |f| %>
  <% if @post.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@post.errors.count, "error") %> prohibited this post from being saved:</h2>

      <ul>
      <% @post.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br>
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_field :description %>
  </div>
  <div class="field">
    <%= f.label :content %><br>
    <%= f.text_area :content %> 
  </div>
  <div class="field">
    <%= f.label :article %>
    <%= f.text_field:article %>
  </div>
  <div class="field">
    <%= f.label :menu %>
    <%= f.text_field :menu %>
  </div>
  <div class="field">
    <% if policy(@post).publish? %><%= f.label :publish? %><%= f.check_box :published %><% end %> 
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
